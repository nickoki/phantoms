  <div class="form">
  <%= form_for solo, url: update_solo_song_url(@song), method: :put do |f| %>
    <% if solo.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(solo.errors.count, "error") %> prohibited this solo from being saved:</h2>

        <ul>
        <% solo.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :is_active, "Active Soloist", value: true %>
      <%= f.radio_button :is_active, true %>
      <%= f.label :is_active, "Past Soloist", value: false %>
      <%= f.radio_button :is_active, false %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
</div>
